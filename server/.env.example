PORT=5000
NODE_ENV=development
# MongoDB connection URI (e.g., from MongoDB Atlas)
# If left empty, the server automatically uses the built-in local JSON database for instant offline testing!
MONGODB_URI=
